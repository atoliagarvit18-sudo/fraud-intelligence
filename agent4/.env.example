# Agent 4 — Fraud Campaign Intelligence Orchestrator
# Copy this to .env and fill in values as needed.

# MongoDB URI — must match Agent 2's .env if using 'mongodb' source mode
MONGO_URI=mongodb://localhost:27017

# Groq API key — optional, only needed for LLM-enhanced narrative mode
# GROQ_API_KEY=your_groq_api_key_here
